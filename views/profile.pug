extends layout

block content
  .col-sm-8.col-sm-offset-2
    .text-center
      h1
        span.fa.fa-user
        | &nbsp;Profile
      br
      span
        strong user id: #{user ? user._id : ''}
        br
        a(href="/user/logout") 
          span.fa.fa-sign-out
          | &nbsp;Sign out
        hr
        .jumbotron
          if (user && user.email != null)
            img.img-circle(src="https://avatars.io/gravatar/" + user.email)
            br
            strong email: #{user.email}
            br
